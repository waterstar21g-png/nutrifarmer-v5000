---
description: V5000 프로젝트 컨텍스트 — 매 채팅마다 자동 로드
globs:
alwaysApply: true
---

# nutrifarmer-v5000 프로젝트 컨텍스트

## 기본 정보
- **프로젝트명**: 탁월한 찬사 — 개인 홈페이지형 블로그 V5000
- **로컬 경로**: `D:/함께온라인/My_BLOG/nutrifarmer-v5000`
- **GitHub**: https://github.com/waterstar21g-png/nutrifarmer-v5000
- **배포 URL**: https://nutrifarmer-v5000.vercel.app
- **원본 참조**: https://www.nutrifarmer.kr/
- **기술 스택**: Next.js 15 + TypeScript + Tailwind CSS + Vercel + **Vercel Postgres** + Cloudflare R2

## 배포 정책 (필수 — 2026-06-27 갱신)
- **코드 변경 작업이 끝나면 사용자에게 묻지 말고 자동 배포** (단, **커밋 선행**)
- 순서: `npm run build` → **git commit** → `vercel --prod --yes` → CHANGELOG·dpl ID 보고
- **예외**: 사용자가 "배포하지 마" / "로컬만" / "커밋하지 마" 명시, 또는 빌드 실패
- **상세**: `.cursor/rules/deploy-git-versioning.mdc`

## 배포 명령
```powershell
cd "D:/함께온라인/My_BLOG/nutrifarmer-v5000"
vercel --prod --yes
```

## 현재 상태 (2026-06-27 기준)
- **프로덕션 (www):** `dpl_CANsJcBgwfh65ryfBipHPduqP35P` · 19:13 KST · tag `prod-canonical-2026-06-27`
- **git:** `d1448c8` + 이후 docs/rule 커밋 — Vercel 59회 배포와 1:1 아님 (CHANGELOG 배포표 참고)
- **진행 중 (v3.0):** V5000 Native Platform — **무 WordPress**
- **확정 spec:** `docs/V5000-native-platform-spec.md`
- CHANGELOG: `docs/CHANGELOG.md` 참조

## V5000 Native Platform — 확정 결정 (2026-06-24)
- **DB:** Vercel Postgres (사용자·글·메타)
- **스토리지:** Cloudflare R2 (미디어)
- **WordPress:** 사용 안 함 — WP REST·브릿지·Docker **금지**
- **회원가입:** 공개 가입 (`/login` register 패널 V5000 내장)
- **WP 계정 이전:** 강제 비밀번호 재설정 (WP 해시 이전 없음)
- **v4000/v3000:** 참고·마이그레이션 소스만 — 코드·env 혼용 금지

## 핵심 설계 원칙
- **단일 진실의 원천**: 카테고리/섹션 데이터는 `lib/site-data.ts` 한 곳에서 관리
- **전체화면 레이아웃**: `--nf-px: clamp(1.25rem, 3.5vw, 3rem)` 패딩 변수 사용
- **max-width 사용 금지**: 섹션 inner 컨테이너는 `width: 100%` + `padding: var(--nf-px)` 방식
- **CSS 변수 체계**: `--nf-primary(#0f2744)`, `--nf-accent(#c05621)`, `--nf-blue(#1a5fa8)` 등

## 네비게이션 고정 8개 카테고리
```
일상 기록 (daily-life) / 가족·성장 (family-growth) / 개인 자료 (personal-archive)
프로그램 (archive-dev) / 삶·사진 (life-photos) / 수익관리 (revenue)
전문 글쓰기 (pro-writing) / 주변 이야기 (fresh-news)
```
- **중요**: WordPress API 카테고리 무시, site-data.ts SHOWCASE_CATS 고정 사용

## 주요 파일 위치
| 파일 | 역할 |
|------|------|
| `app/globals.css` | 전역 CSS (--nf-* 변수 체계) |
| `lib/site-data.ts` | 카테고리/섹션 고정 데이터 |
| `lib/wordpress.ts` | ⚠️ **제거 예정** — v3.0에서 `lib/v5000-content.ts` 로 대체 |
| `docs/V5000-native-platform-spec.md` | Native Platform 확정 spec |
| `components/SiteHeader.tsx` | 상단 네비게이션 |
| `components/HeroSlider.tsx` | 드래그 슬라이더 |
| `components/StatsSection.tsx` | 통계 카운터 |
| `components/write/WriteEditor.tsx` | AI 글쓰기 에디터 |
| `app/write/write.css` | 에디터 전용 스타일 |

## WordPress — 사용 안 함 (v3.0)
- **런타임:** WP REST·WP auth·`/api/wp/*` **신규 코드 금지**, 제거 진행
- **마이그레이션:** 1회 export 스크립트만 허용 (`scripts/v5000-migrate-from-wp.ts` 예정)
- **과도기:** localStorage 초안·로컬 AI 시뮬레이션은 유지 가능 (WP 폴백 아님)

## 코딩 규칙
- 컴포넌트 파일: 50~200줄 유지, 기능별 분리
- CSS 클래스: `nf-` 접두사 (사이트 전역), `nfw-` 접두사 (write 에디터)
- 이미지: Next.js `<Image>` 컴포넌트 사용, `next.config.ts` 도메인 허용 목록 확인
- 배포 전: `npm run build` 빌드 검증 필수
